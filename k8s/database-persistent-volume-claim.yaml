apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
  namespace: default
  labels:
    app: edentmysql

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
