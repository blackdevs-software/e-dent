apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default
  labels:
    app: edent

spec:
  # type: LoadBalancer
  type: ClusterIP
  # clusterIP: 10.96.0.2

  selector:
    app: edent
    tier: client

  ports:
    - port: 80
      # targetPort: 80
      # protocol: TCP

# status:
#   loadBalancer:
#     ingress:
#     - ip: 192.168.0.100
