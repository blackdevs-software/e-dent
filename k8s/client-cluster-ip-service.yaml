apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default

spec:
  type: ClusterIP

  selector:
    app: edentclient

  ports:
    - port: 80
      targetPort: client-port
      protocol: TCP
